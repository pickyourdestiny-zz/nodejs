const num1 = 5
const num2 = 10

function addValues() {
    console.log(`the sum is : ${num1 + num2}`)
}

addValues()   // this function is invoked in a module, therefore if you require it it will be invoked automatically.
